@model IEnumerable<MvcMovie.Models.ViewModels.StudentVM>

@{
    ViewData["Title"]="Index";
}
<h1>Index</h1>
<a asp-action="Create">Create new</a>
<table>
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model=>model.FullName)
            </th>
            <th>
                @Html.DisplayNameFor(model=>model.StudentCode)
            </th>
            <th>
                @Html.DisplayNameFor(model=>model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FacultyName)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model){
            <tr>
                <td>
                    @Html.DisplayFor(modelItem=>item.FullName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem=>item.StudentCode)
                </td>
                <td>
                    @Html.DisplayFor(modelItem=>item.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FacultyName)
                </td>
                <td>
                <a asp-action="Edit" asp-route-id="@item.StudentCode" class="btn btn-warning">Edit</a>
                <a asp-action="Details" asp-route-id="@item.StudentCode" class="btn btn-success">Details</a>
                <a asp-action="Delete" asp-route-id="@item.StudentCode" class="btn btn-danger">Delete</a>
            </td>
            </tr>
        }
    </tbody>
</table>