@model MvcMovie.Models.ViewModels.StudentVM

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Student</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.FullName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.FullName)
        </dd>
           <dt class="col-sm-2">@Html.DisplayNameFor(model=>model.StudentCode)</dt>
    <dt class="col-sm-10">@Html.DisplayFor(model=>model.StudentCode)</dt>
    <dt class="col-sm-2">@Html.DisplayNameFor(model=>model.Email)</dt>
    <dt class="col-sm-10">@Html.DisplayFor(model=>model.Email)</dt>
    <dt class="col-sm-2">@Html.DisplayNameFor(model=>model.FacultyName)</dt>
    <dt class="col-sm-10">@Html.DisplayFor(model=>model.FacultyName)</dt>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.StudentCode">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>